package main

import (
	"fmt"
)

func main() {
	fmt.Println("Hello, World!") //is a statement.
}

/**
- every program is part of a package
- "Executable code" means code that can be compiled into a program and run directly, which requires the main package and the main() function in Go.
- mở ngoặc nhọn không thể đứng đầu dòng

*/
